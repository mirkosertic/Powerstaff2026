package de.mirkosertic.powerstaff.profilesearch.api;

//import org.springframework.ai.chat.client.ChatClient;
//import org.springframework.ai.chat.memory.ChatMemory;
//import org.springframework.ai.tool.ToolCallbackProvider;
import org.springframework.stereotype.Component;

//@Component
public class PSChatClient {

/*    private final ChatClient chatClient;

    public PSChatClient(final ChatClient.Builder builder,
                        final ToolCallbackProvider mcpTools,
                        final ChatMemory chatMemory) {
        chatClient = builder
                //.defaultTools(mcpTools.getToolCallbacks())   // alle MCP-Tools automatisch registriert
                .defaultSystem("Du bist ein hilfreicher Assistent mit Zugang zu meinem Lucene-Index.")
                //.defaultAdvisors(new MessageChatMemoryAdvisor(chatMemory))
                .build();
    }*/
}
